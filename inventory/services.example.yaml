---
domain: home.arpa
services:
  dashboard:
    hostname: dashboard
    protocol: http
    upstream: homepage:3000
    health_path: /
    public_on_lan: true
  photos:
    hostname: photos
    protocol: http
    upstream: 192.168.1.20:2283
    health_path: /
    public_on_lan: true

