# Backup and restore

Implementation is deferred to Phase 8. Backups must include Caddy data and its
local CA, Uptime Kuma state, Pi-hole configuration, RustDesk keys, inventory,
and the Git repository. Any archive containing private material must be
encrypted and a restore test must be recorded.

