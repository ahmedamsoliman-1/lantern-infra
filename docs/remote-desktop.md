# Remote desktop

RustDesk Server OSS deployment is deferred to Phase 7. It will be LAN-only and
its keys will be stored outside Git and included in encrypted backups. Windows
11 Pro can also host RDP, but RDP was disabled during discovery and remains so.

