# Portainer

Portainer is optional and is not part of the Phase 1 Compose skeleton. If added,
Git and Docker Compose remain the source of truth; never expose the Docker socket
without documenting and accepting its host-level security implications.

